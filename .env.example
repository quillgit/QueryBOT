# Server
PORT=3000
# Use a strong, random string in production
JWT_SECRET=change-me-to-a-strong-random-secret

# Admin credentials for app login (JWT)
ADMIN_USER=admin
ADMIN_PASS=password

# Database
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=target_database

# Frontend (optional override)
# In production, frontend defaults to same-origin "/api"
# Set when UI and API are on different hosts
# VITE_API_BASE=https://api.example.com/api

# Notes:
# - Copy this file to .env and adjust values for your environment.
# - Keep .env out of version control (see .gitignore).
# - Ensure your DB user has privileges on DB_NAME (CREATE/SELECT/INSERT/DELETE).